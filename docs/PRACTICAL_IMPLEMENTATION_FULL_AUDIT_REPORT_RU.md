# Полный аудит практической части дипломной работы

Дата проверки: 1 июня 2026 года.

## 1. Итоговый вердикт

Практическая часть содержит большой объем воспроизводимого кода и дает
защищаемый исследовательский результат:

- реализована и проверена арифметика MNT4-753 и MNT6-753;
- построен полный on-chain baseline для MNT4-753;
- реализован оптимизированный MNT4-верификатор уравнения сопряжений по мотивам
  ePrint 2024/640;
- экспериментально подтверждено, что оптимизация финальной экспоненты снижает
  стоимость, но не устраняет доминирующую стоимость цикла Миллера;
- исследованы KZG и Merkle/FRI как способы сжатия проверки цикла Миллера;
- реализован воспроизводимый Merkle/FRI cost model и сохранен исполняемый
  DEEP-FRI отрицательный эксперимент;
- реализованы арифметические прототипы MNT4/MNT6-цикла и lollipop-305.

Однако исходный сильный тезис «готова production-библиотека, которая для
произвольного недоверенного кэша дешево заменяет MNT-сопряжение в EVM» заявлять
нельзя. Основные причины:

1. Самый дешевый MNT4 fixed-shards verifier использует доверенную регистрацию
   подготовленного кэша при развертывании контракта.
2. MNT-cycle constraints-модуль является моделью количества умножений, а не
   полностью скомпилированной системой ограничений.
3. Merkle/FRI-направление честно завершено как аналитический и отрицательный
   эксперимент, а не как production-замена on-chain цикла Миллера.

Следовательно, работа готова к представлению как всестороннее исследование
границ применимости MNT-арифметики в EVM и как набор проверенных прототипов.
Production-готовность произвольного недоверенного кэша остается отдельной
задачей следующего этапа.

## 2. Проверенный состав проекта

| Модуль | Назначение | Статус |
|---|---|---|
| `arithmetic/mnt4_3limb` | Оптимизированная 3-limb арифметика MNT4 | Закрыто кодом |
| `arithmetic/mnt6_3limb` | Оптимизированная 3-limb арифметика MNT6 | Закрыто кодом |
| `arithmetic/lollipop305_2limb` | Исследовательская 2-limb арифметика | Закрыто кодом |
| `implementations/full_onchain_mnt4` | Полное on-chain MNT4-сопряжение | Закрыто кодом |
| `implementations/article640_mnt4` | MNT4 Article640 direct residue verifier | Закрыто для фиксированной доверенной конфигурации |
| `implementations/article640_mnt6` | MNT6 packed Miller, FE и fixed-shards bool verifier | Закрыто для фиксированной доверенной конфигурации |
| `mnt_cycle_full` | Модель стоимости MNT4/MNT6 cycle-native слоя | Закрыто как ручная модель |
| `implementations/lollipop305` | Исследовательские lollipop-305 прототипы | Закрыто как исследовательский прототип |
| `implementations/mnt4_merkle_fri_cost_model` | Ordinary-FRI stop/go cost model | Закрыто аналитически |
| `implementations/research_variants/mnt4_merkle_deep_fri_microtrace` | Исполняемый DEEP-FRI отрицательный эксперимент | Архив |
| `baselines/naive_tate_mnt4` | Наивная модель стоимости Tate-пути | Закрыто как микроблоки с экстраполяцией |

## 3. Выполненные проверки

### 3.1. Канонический запуск

Выполнена команда:

```bash
./scripts/run_all.sh
```

Команда завершилась успешно и покрыла арифметику, полный MNT4 baseline,
Article640 MNT4/MNT6, lollipop-305, Rust backend-ы, MNT-cycle модель и
актуальную ordinary-FRI модель стоимости.

### 3.2. Расширенные Foundry-проверки

| Модуль | Команда | Результат |
|---|---|---:|
| MNT4 arithmetic | `cd arithmetic/mnt4_3limb && forge test -vv` | 72 теста пройдено |
| MNT6 arithmetic | `cd arithmetic/mnt6_3limb && forge test -vv` | 13 тестов пройдено |
| lollipop 2-limb | `cd arithmetic/lollipop305_2limb && forge test -vv` | 22 теста пройдено |
| Full on-chain MNT4 | `cd implementations/full_onchain_mnt4 && forge test -vv` | 116 тестов пройдено |
| Article640 MNT4 | `cd implementations/article640_mnt4 && forge test -vv` | 31 тест пройден |
| Article640 MNT6 | `cd implementations/article640_mnt6 && forge test -vv` | 13 тестов пройдено |
| lollipop-305 | `cd implementations/lollipop305 && forge test -vv` | 31 тест пройден |
| DEEP-FRI archive | `cd implementations/research_variants/mnt4_merkle_deep_fri_microtrace && ./scripts/run_report.sh` | Rust 9/9, Foundry 21/21 |

Full on-chain MNT4 дополнительно сверяется с arkworks: проверяются базовое поле,
расширения, подготовленные линии, цикл Миллера, fixed-Q и parametric-Q режимы,
этапы финальной экспоненты и итоговое значение.

### 3.3. Воспроизводимость fixtures

- MNT6 Rust fixture побайтно совпал с tracked JSON.
- MNT4 Rust backend воспроизвел tracked `article640_hot.words.hex`.
- lollipop fixture-ы совпали побайтно либо после удаления завершающего перевода
  строки.
- Bash-скрипты прошли `bash -n`.
- В git не отслеживаются `target`, `out`, `cache`, `node_modules`, `.reports`.

### 3.4. Проверка Merkle/FRI направления

Ordinary-FRI модель:

```bash
cd implementations/mnt4_merkle_fri_cost_model
./scripts/run_cost_model.sh
```

Получены:

| Показатель | Значение |
|---|---:|
| Строгая нижняя оценка | 51 359 352 gas |
| Ожидаемая модельная оценка | 78 340 624 gas |
| Обязательная calldata | 2 072 224 байта |
| Число запросов | 576 |
| FRI schedule | `[2, 2, 2, 4]` |

Модель подтверждает отрицательный граничный результат: обычный Merkle/FRI
подход не дает принципиального выигрыша относительно fixed-shards Article640.

Архивный DEEP-FRI микротрассировочный verifier воспроизводимо исполняется, но
не является production-профилем:

| Профиль | Размер доказательства | Function gas | Примечание |
|---|---:|---:|---|
| 32 запроса | 263 612 байт | 83 962 252 | Исследовательский профиль |
| 128 запросов | 953 468 байт | 640 161 168 | Консервативный эксперимент |

Для этих профилей не зафиксирована численная DEEP-FRI теорема soundness.

## 4. Подтвержденные gas-метрики

### 4.1. Базовая арифметика

| Операция | Gas/op |
|---|---:|
| MNT4 `Fq` умножение | 2 959 |
| MNT4 `Fq` возведение в квадрат | 2 947 |
| MNT6 `Fp` умножение без overhead | 2 314 |
| MNT6 `Fp` возведение в квадрат без overhead | 2 301 |
| MNT6 `Fq3` умножение | 36 472 |
| MNT6 `Fq3` возведение в квадрат | 26 813 |
| MNT6 `Fq6` умножение | 166 774 |
| MNT6 `Fq6` возведение в квадрат | 113 788 |

### 4.2. MNT4

| Сценарий | Gas |
|---|---:|
| Полное on-chain fixed-Q вычисление | 259 346 808 |
| Article640 hot full equation | 152 791 708 |
| Article640 hot residue equation | 130 690 504 |
| Article640 hot residue code-shards | 93 807 370 |
| Article640 fixed-shards function gas с проверкой G1 | 93 734 789 |
| Prepared sparse code-shards digest | 80 188 440 |

Самый дешевый безопасный для повторного использования runtime-режим фиксирует
адреса code-shards в конструкторе и не принимает их от пользователя при каждом
вызове. Его безопасность зависит от корректной регистрации кэша при
развертывании.

### 4.3. MNT6

| Сценарий | Gas |
|---|---:|
| Полный packed путь | 312 184 436 |
| Packed FE path | 131 685 843 |
| Packed residue digest одного сопряжения | 103 277 505 |
| Packed FE isolated | 38 428 108 |
| Generic FE | 218 388 832 |
| Fixed-shards bool equation с полной packed FE и проверкой G1 | 226 078 963 |
| Fixed-shards bool residue equation с общим аккумулятором и проверкой G1 | 172 004 717 |

Последняя строка является сопоставимым с MNT4 основным fixed-shards режимом:
контракт фиксирует адреса code-shards, потоково читает prepared coefficients,
проверяет входные G1-точки и использует один общий multi-Miller аккумулятор:
возведение в квадрат выполняется один раз на раунд, после чего в аккумулятор
последовательно домножаются линии для обеих пар. Полная финальная экспонента
заменена Article640-проверкой с `c`-свидетельством. Для MNT6 знак отличается от
MNT4: из `r_MNT6=q_MNT6-N` следует `c^{-r}=c^{N-q}`. Экономия относительно
контрольного режима с полной FE составляет `54 074 246 gas`, или `23.92%`.

### 4.4. lollipop-305

| Кривая и режим | Test-call gas |
|---|---:|
| Stick: Miller core | 9 265 790 |
| Stick: residue | 12 675 796 |
| Stick: direct FE | 34 329 327 |
| Cycle E: Miller core | 19 181 838 |
| Cycle E: residue | 26 320 346 |
| Cycle E: direct FE | 42 121 211 |
| Ehat: ate residue | 113 401 094 |
| Ehat: Weil | 247 606 911 |
| Combined fixed-shards | 133 756 330 |

lollipop-305 демонстрирует пользу меньшего числа limb-ов, но не дает готовую
production-кривую достаточного уровня безопасности.

## 5. Статус исправлений и сохраняющиеся границы

### P1. Доверенная регистрация кэша в MNT4 fixed-shards

`MNT4Article640FixedShardsVerifier` фиксирует адреса data-контрактов в
конструкторе и затем читает линии только из них. Это защищает от подмены кэша
пользователем во время вызова, но не доказывает, что при развертывании были
зарегистрированы именно корректные линии для Q и S.

Commitment-режим имеет ту же границу: хеш связывает вызов с зарегистрированным
blob, но сам по себе не доказывает корректность генерации линий.

**Принятая модель:** в рамках диплома используется фиксированная доверенная
конфигурация. Это осознанное ограничение предмета исследования, а не скрытая
гарантия trustless-регистрации.

1. Для fixed-Q сервиса: считать регистрацию доверенной конфигурацией, добавить
   deployment-скрипт, который воспроизводит Rust fixture и проверяет хеши.
2. Для универсального trustless API: добавить отдельное доказательство
   корректности `Q -> lines -> commitment`. Оно неизбежно потребует новых
   gas/constraints измерений.

### Исправлено. Проверка точек на кривой и в подгруппе

Публичные MNT4 и MNT6 fixed-shards API теперь отклоняют неканонические
координаты и точки вне G1. Для обеих используемых G1-групп cofactor равен
единице, поэтому проверка уравнения кривой одновременно задает subgroup policy.
Добавлены отрицательные тесты с точками вне G1.

### Исправлено. MNT6 fixed-shards bool verifier

Добавлен `MNT6Article640FixedShardsVerifier`: он хранит shard-адреса, потоково
читает prepared cache через `EXTCODECOPY`, проверяет G1-точки и возвращает
`bool` для полного уравнения сопряжений. Rust backend генерирует ненулевой
arkworks-вектор `P=2G1`, `R=G1`, `Q=G2`, `S=2G2`.

### Исправлено. Счетчики MNT-cycle модели

В `mnt_cycle_full` использованы счетчики `377/124` для обеих кривых. Для
исполняемых циклов нужны:

| Кривая | Удвоения | Сложения |
|---|---:|---:|
| MNT4 | 376 | 124 |
| MNT6 | 376 | 123 |

После исправления текущей формулы модель дает:

| Слой | Было | Должно быть |
|---|---:|---:|
| MNT4 prepared relation | 24 178 | 24 126 |
| MNT6 prepared relation | 49 120 | 48 942 |

Кроме того, это ручная модель multiplication constraints, а не полностью
скомпилированная constraint system.

Метрики во всех документах называются модельной оценкой multiplication
constraints. Для более сильного тезиса потребуется отдельная реализация
скомпилированных gadgets.

### P1. Merkle/FRI не реализован как production-замена цикла Миллера

Основной каталог содержит корректно размеченную ordinary-FRI модель стоимости.
В Article640 реализован Merkle opening layer, но не полный low-degree FRI
verifier. Архивный DEEP-FRI прототип полезен как отрицательный эксперимент, но
не закрывает production soundness.

**Как исправить:** не заявлять полноценную Merkle/FRI замену. Для продолжения
ветки сначала численно выбрать soundness-профиль по конкретной теореме, затем
реализовать оптимизированный verifier. Текущая cost model показывает, что это
направление не является приоритетным.

### Уточнено. Наивная Tate cost model

Модуль `baselines/naive_tate_mnt4` теперь явно называется моделью стоимости. Он
измеряет школьные `Fq/Fq2/Fq4` операции, общий шаг Миллера и 16-битный фрагмент
обычного бинарного возведения в степень. Полный неоптимизированный Tate-вызов
не заявляется измеренным: итог порядка `2.55B gas` является строгой нижней
экстраполяцией измеренных микроблоков. Полный исполняемый reference-контур
находится в `implementations/full_onchain_mnt4`.

### Исправлено. MNT6 streaming code-shards hot path

Добавлен потоковый code-shards путь: коэффициенты читаются небольшими блоками
через `EXTCODECOPY` непосредственно в scratch memory, без копирования всего
prepared cache. Fixed-shards тест измеряет завершенный bool verifier.

### Исправлено. Корневой ordinary-FRI runner

`scripts/run_all.sh` теперь вызывает `run_mnt4_merkle_fri_cost_model.sh`.
Актуальная модель обычного FRI входит в единый воспроизводимый запуск.

### P2. Документация не полностью синхронизирована

В документах остались абсолютные пути старого репозитория, прежние имена
Merkle/FRI модулей и старые числа MNT-cycle модели.

**Как исправить:** выполнить редакторский проход после исправления счетчиков,
оставить относительные пути и единую таблицу статусов.

## 6. Проверка размеров контрактов

Лимит EIP-170 равен 24 576 байт runtime-кода.

| Контракт | Runtime bytes | Развертываем |
|---|---:|---|
| MNT4 fixed-shards verifier | 17 166 | Да |
| MNT4 commitment verifier | 20 627 | Да |
| MNT6 fixed-shards verifier | 15 891 | Да |
| MNT6 direct diagnostic verifier | 21 732 | Да |
| lollipop main verifier | 14 154 | Да |
| MNT4 direct hot harness | 30 857 | Нет |
| MNT4 pairing modes bench | 25 647 | Нет |
| Архивный DEEP-FRI verifier | 28 045 | Нет |

Неразвертываемые контракты допустимы только как тестовые harness-ы или архивные
эксперименты.

## 7. Сравнение с Sonobe

Официальный README Sonobe указывает ориентир порядка 9 млн ограничений для
CycleFold decider и время порядка трех минут на указанной авторами машине.
Локальные MNT-cycle числа 24 126 и 48 942 нельзя напрямую делить на 9 млн:
они описывают подготовленные relation-фрагменты, а Sonobe-число относится к
целому decider-контуру.

Корректная формулировка результата:

> MNT4/MNT6 cycle-native арифметика дает перспективный малый строительный блок
> для будущего folding. Прямое количественное превосходство над полным Sonobe
> decider еще не доказано, потому что в проекте не собран полный folding-контур.

## 8. Оставшиеся возможности оптимизации

### MNT4

Для прямого исполнения в EVM исследованы Montgomery/CIOS, Barrett, FIOS,
Comba/SOS, branchless reduction, lazy reduction, Karatsuba, sparse lines,
fused hot path, blob и code-shards режимы. Очевидного технического улучшения
более чем на 10% без смены архитектуры не найдено.

### MNT6

Потоковый code-shards режим, packed pointer API и scratch memory реализованы.
Дополнительный all-stack Yul для отдельных `Fq3/Fq6` микроблоков может дать
локальные проценты, но не меняет порядок стоимости двух Miller loop.

### Архитектурная граница

Для существенного выигрыша нужно менять не отдельную Yul-функцию, а способ
проверки цикла Миллера: precompile, доказательство корректности кэша,
folding-native relation или компактная полиномиальная схема с приемлемым PCS.

## 9. Оставшиеся задачи вне текущего DoD

### Отдельная будущая исследовательская работа

1. Скомпилировать полноценные MNT-cycle constraint gadgets.
2. Если Merkle/FRI направление продолжается, сначала закрыть численный
   soundness-профиль и только затем оптимизировать Solidity verifier.
3. Для универсального недоверенного кэша реализовать доказательство
   `Q -> lines -> commitment`.

## 10. Источники

- [On Proving Pairings, ePrint 2024/640](https://eprint.iacr.org/2024/640.pdf)
- [Cycles of pairing-friendly elliptic curves, ePrint 2024/1627](https://eprint.iacr.org/2024/1627.pdf)
- [ark-mnt4-753 documentation](https://docs.rs/ark-mnt4-753/latest/ark_mnt4_753/)
- [Sonobe repository](https://github.com/privacy-ethereum/sonobe)
- [EIP-170: Contract code size limit](https://eips.ethereum.org/EIPS/eip-170)
